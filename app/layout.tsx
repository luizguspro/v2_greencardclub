import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Greencardclub - Carteirinha Digital Cannabis Medicinal",
  description: "Obtenha sua carteirinha digital para uso de cannabis medicinal em 5 minutos.",
  keywords: "cannabis medicinal, carteirinha digital, greencardclub",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="pt-BR">
      <body className={inter.className}>
        <div className="min-h-screen flex flex-col">
          {children}
        </div>
      </body>
    </html>
  );
}