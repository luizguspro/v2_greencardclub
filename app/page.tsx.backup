"use client";

import { useState, useEffect } from "react";
import { motion } from "framer-motion";
import { Leaf, Shield, Clock, Users, ArrowRight, CheckCircle, Sparkles } from "lucide-react";
import Link from "next/link";
import Card3D from "@/components/3d/Card3D";

export default function Home() {
  const [isHovered, setIsHovered] = useState(false);

  const stats = [
    { value: "1.234", label: "Usuários Ativos", icon: Users },
    { value: "5min", label: "Tempo Médio", icon: Clock },
    { value: "100%", label: "Digital", icon: CheckCircle }
  ];

  return (
    <div className="min-h-screen overflow-x-hidden">
      {/* Gradient Background */}
      <div className="fixed inset-0 bg-gradient-to-br from-greencard-background via-white to-greencard-tertiary/20 -z-10" />
      
      {/* Animated Background Elements */}
      <div className="fixed inset-0 -z-10 overflow-hidden">
        <div className="absolute -top-40 -left-40 w-80 h-80 bg-greencard-secondary/20 rounded-full blur-3xl animate-pulse" />
        <div className="absolute top-1/2 -right-40 w-96 h-96 bg-greencard-tertiary/30 rounded-full blur-3xl animate-pulse" />
        <div className="absolute bottom-0 left-1/3 w-72 h-72 bg-greencard-secondary/10 rounded-full blur-3xl animate-pulse" />
      </div>

      {/* Header */}
      <motion.header 
        initial={{ y: -100 }}
        animate={{ y: 0 }}
        transition={{ duration: 0.5 }}
        className="sticky top-0 z-50 bg-white/80 backdrop-blur-xl border-b border-greencard-tertiary/30"
      >
        <nav className="container mx-auto px-6 py-4">
          <div className="flex items-center justify-between">
            <Link href="/" className="flex items-center gap-3 group">
              <motion.div
                animate={{ rotate: isHovered ? 360 : 0 }}
                transition={{ duration: 0.5 }}
                onHoverStart={() => setIsHovered(true)}
                onHoverEnd={() => setIsHovered(false)}
                className="relative"
              >
                <Leaf className="h-10 w-10 text-greencard-primary" />
                <div className="absolute inset-0 bg-greencard-secondary/30 rounded-full blur-xl scale-150 opacity-0 group-hover:opacity-100 transition-opacity" />
              </motion.div>
              <div>
                <span className="text-3xl font-black tracking-tight">
                  GREENCARD<span className="text-greencard-secondary italic font-light">club</span>
                </span>
              </div>
            </Link>
            
            <div className="flex items-center gap-6">
              <Link 
                href="/login" 
                className="text-greencard-primary hover:text-greencard-secondary font-semibold transition-colors"
              >
                Entrar
              </Link>
              <Link 
                href="/carteirinha" 
                className="bg-greencard-primary hover:bg-greencard-primary/90 text-white font-bold px-8 py-3 rounded-full transition-all hover:shadow-xl hover:scale-105 flex items-center gap-2"
              >
                Criar Carteirinha
                <ArrowRight className="h-4 w-4" />
              </Link>
            </div>
          </div>
        </nav>
      </motion.header>

      {/* Hero Section with 3D Card */}
      <section className="relative min-h-[90vh] flex items-center py-20">
        <div className="container mx-auto px-6">
          <div className="grid lg:grid-cols-2 gap-12 items-center">
            {/* Content */}
            <motion.div
              initial={{ opacity: 0, x: -50 }}
              animate={{ opacity: 1, x: 0 }}
              transition={{ duration: 0.6 }}
            >
              <motion.div
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: 0.2 }}
                className="inline-flex items-center gap-2 px-4 py-2 bg-greencard-secondary/20 backdrop-blur-sm rounded-full mb-8"
              >
                <Shield className="h-5 w-5 text-greencard-primary" />
                <span className="text-sm font-bold text-greencard-primary">100% Legal e Seguro</span>
              </motion.div>
              
              <h1 className="text-5xl lg:text-7xl font-black leading-tight mb-6">
                Sua Carteirinha Digital para{" "}
                <span className="text-transparent bg-clip-text bg-gradient-to-r from-greencard-primary to-greencard-secondary">
                  Cannabis Medicinal
                </span>
              </h1>
              
              <p className="text-xl text-gray-600 mb-10 leading-relaxed">
                Cadastre-se, obtenha sua autorização digital e tenha acesso legal ao tratamento. 
                Processo 100% online, rápido e seguro.
              </p>
              
              <div className="flex flex-col sm:flex-row gap-4">
                <motion.div whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}>
                  <Link 
                    href="/carteirinha" 
                    className="group bg-gradient-to-r from-greencard-primary to-greencard-primary/80 hover:from-greencard-primary hover:to-greencard-primary text-white font-bold text-lg px-10 py-4 rounded-full transition-all shadow-xl hover:shadow-2xl flex items-center gap-3 justify-center"
                  >
                    Criar Carteirinha Agora
                    <ArrowRight className="h-5 w-5" />
                  </Link>
                </motion.div>
                <motion.div whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}>
                  <Link 
                    href="/login" 
                    className="bg-white hover:bg-gray-50 text-greencard-primary font-bold text-lg px-10 py-4 rounded-full transition-all border-2 border-greencard-primary shadow-lg hover:shadow-xl flex items-center gap-2 justify-center"
                  >
                    Já Tenho Conta
                  </Link>
                </motion.div>
              </div>
            </motion.div>

            {/* 3D Card Mockup */}
            <motion.div
              initial={{ opacity: 0, scale: 0.8 }}
              animate={{ opacity: 1, scale: 1 }}
              transition={{ delay: 0.3, duration: 0.6 }}
              className="relative flex justify-center lg:justify-end"
            >
              <div className="bg-gradient-to-br from-greencard-primary to-greencard-primary/90 rounded-3xl p-8 text-white shadow-2xl">
                <h3 className="text-2xl font-black mb-4">GREENCARD<span className="text-greencard-secondary italic">club</span></h3>
                <p className="text-sm opacity-80 mb-6">CARTEIRA DIGITAL</p>
                <div className="w-32 h-40 bg-white/20 rounded-lg mb-4"></div>
                <p className="font-bold">SEU NOME AQUI</p>
                <p className="text-sm opacity-80">CPF: ***.***.***-**</p>
                <p className="text-sm opacity-80">Válido até: 12/2025</p>
              </div>
            </motion.div>
          </div>

          {/* Stats */}
          <motion.div
            initial={{ opacity: 0, y: 50 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.6 }}
            className="grid grid-cols-3 gap-6 max-w-2xl mt-20"
          >
            {stats.map((stat, index) => (
              <motion.div
                key={index}
                initial={{ opacity: 0, scale: 0.5 }}
                animate={{ opacity: 1, scale: 1 }}
                transition={{ delay: 0.7 + index * 0.1 }}
                whileHover={{ scale: 1.05 }}
                className="relative group"
              >
                <div className="absolute inset-0 bg-gradient-to-r from-greencard-secondary to-greencard-tertiary rounded-2xl blur-xl opacity-30 group-hover:opacity-50 transition-opacity" />
                <div className="relative bg-white/80 backdrop-blur-sm rounded-2xl p-6 border border-greencard-tertiary/30">
                  <stat.icon className="h-8 w-8 text-greencard-primary mb-2" />
                  <p className="text-3xl font-black text-greencard-primary">{stat.value}</p>
                  <p className="text-sm text-gray-600 font-medium">{stat.label}</p>
                </div>
              </motion.div>
            ))}
          </motion.div>
        </div>
      </section>

      {/* Features Section */}
      <section className="py-20 relative">
        <div className="container mx-auto px-6">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl lg:text-5xl font-black mb-4">
              Por que escolher o Greencard<span className="text-greencard-secondary italic">club</span>?
            </h2>
            <p className="text-xl text-gray-600">
              A forma mais simples e segura de obter sua autorização
            </p>
          </motion.div>
          
          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {[
              {
                icon: Shield,
                title: "100% Legal",
                description: "Processo totalmente regulamentado e conforme a legislação vigente",
                gradient: "from-green-400 to-green-600"
              },
              {
                icon: Clock,
                title: "Rápido e Simples",
                description: "Carteirinha digital pronta em apenas 5 minutos, sem burocracia",
                gradient: "from-emerald-400 to-emerald-600"
              },
              {
                icon: Users,
                title: "Suporte Médico",
                description: "Médicos especializados e suporte humanizado durante todo processo",
                gradient: "from-teal-400 to-teal-600"
              }
            ].map((feature, index) => (
              <motion.div
                key={index}
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ delay: index * 0.2 }}
                viewport={{ once: true }}
                whileHover={{ y: -10 }}
                className="group relative"
              >
                <div className="absolute inset-0 bg-gradient-to-r from-greencard-secondary to-greencard-tertiary rounded-3xl blur-xl opacity-0 group-hover:opacity-50 transition-opacity duration-500" />
                <div className="relative bg-white/80 backdrop-blur-xl rounded-3xl p-8 border border-greencard-tertiary/30 hover:border-greencard-secondary/50 transition-all">
                  <div className={`w-16 h-16 bg-gradient-to-r ${feature.gradient} rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform`}>
                    <feature.icon className="h-8 w-8 text-white" />
                  </div>
                  <h3 className="text-2xl font-bold mb-3 text-greencard-primary">
                    {feature.title}
                  </h3>
                  <p className="text-gray-600 leading-relaxed">
                    {feature.description}
                  </p>
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* How it Works */}
      <section className="py-20 bg-gradient-to-b from-white to-greencard-background/30">
        <div className="container mx-auto px-6">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl lg:text-5xl font-black mb-4">
              Como funciona?
            </h2>
            <p className="text-xl text-gray-600">
              Processo simples em 4 passos
            </p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-4 gap-8">
            {[
              { step: "1", title: "Cadastro", desc: "Preencha seus dados pessoais" },
              { step: "2", title: "Foto", desc: "Adicione sua foto para a carteirinha" },
              { step: "3", title: "Revisão", desc: "Confirme todas as informações" },
              { step: "4", title: "Pronto!", desc: "Baixe sua carteirinha digital" }
            ].map((item, index) => (
              <motion.div
                key={index}
                initial={{ opacity: 0, scale: 0.8 }}
                whileInView={{ opacity: 1, scale: 1 }}
                transition={{ delay: index * 0.1 }}
                viewport={{ once: true }}
                className="relative"
              >
                {index < 3 && (
                  <div className="hidden md:block absolute top-12 left-full w-full h-0.5 bg-gradient-to-r from-greencard-secondary to-transparent" />
                )}
                <div className="text-center">
                  <motion.div
                    whileHover={{ scale: 1.1, rotate: 360 }}
                    transition={{ duration: 0.5 }}
                    className="w-24 h-24 mx-auto bg-gradient-to-br from-greencard-secondary to-greencard-tertiary rounded-full flex items-center justify-center mb-4"
                  >
                    <span className="text-3xl font-black text-greencard-primary">
                      {item.step}
                    </span>
                  </motion.div>
                  <h3 className="text-xl font-bold mb-2 text-greencard-primary">
                    {item.title}
                  </h3>
                  <p className="text-gray-600">
                    {item.desc}
                  </p>
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* CTA Section */}
      <section className="py-20 relative overflow-hidden">
        <div className="absolute inset-0 bg-gradient-to-r from-greencard-primary to-greencard-primary/90" />
        
        <div className="container mx-auto px-6 relative z-10">
          <motion.div
            initial={{ opacity: 0, scale: 0.9 }}
            whileInView={{ opacity: 1, scale: 1 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
            className="text-center text-white max-w-3xl mx-auto"
          >
            <Sparkles className="h-16 w-16 mx-auto mb-6" />
            <h2 className="text-4xl lg:text-5xl font-black mb-6">
              Pronto para começar sua jornada?
            </h2>
            <p className="text-xl mb-10 opacity-90">
              Junte-se a milhares de pessoas que já têm acesso legal ao tratamento com cannabis medicinal
            </p>
            <motion.div
              whileHover={{ scale: 1.05 }}
              whileTap={{ scale: 0.95 }}
              className="inline-block"
            >
              <Link 
                href="/carteirinha" 
                className="bg-white hover:bg-gray-100 text-greencard-primary font-black text-lg px-12 py-5 rounded-full transition-all shadow-2xl flex items-center gap-3"
              >
                Criar Minha Carteirinha
                <ArrowRight className="h-5 w-5" />
              </Link>
            </motion.div>
          </motion.div>
        </div>
      </section>

      {/* Footer */}
      <footer className="bg-greencard-primary text-white py-16">
        <div className="container mx-auto px-6">
          <div className="grid grid-cols-1 md:grid-cols-4 gap-8 mb-8">
            <div className="col-span-1 md:col-span-2">
              <div className="flex items-center gap-3 mb-4">
                <Leaf className="h-8 w-8" />
                <span className="text-2xl font-bold">GREENCARDCLUB</span>
              </div>
              <p className="text-sm opacity-80 mb-4">
                Tornando simples o que deveria ser. Sistema de carteirinha digital para cannabis medicinal 100% online e seguro.
              </p>
            </div>
            
            <div>
              <h4 className="font-bold mb-4">Links Úteis</h4>
              <ul className="space-y-2 text-sm opacity-80">
                <li><Link href="/sobre" className="hover:opacity-100 transition-opacity">Sobre</Link></li>
                <li><Link href="/como-funciona" className="hover:opacity-100 transition-opacity">Como funciona</Link></li>
                <li><Link href="/medicos" className="hover:opacity-100 transition-opacity">Médicos</Link></li>
                <li><Link href="/faq" className="hover:opacity-100 transition-opacity">FAQ</Link></li>
              </ul>
            </div>
            
            <div>
              <h4 className="font-bold mb-4">Legal</h4>
              <ul className="space-y-2 text-sm opacity-80">
                <li><Link href="/privacidade" className="hover:opacity-100 transition-opacity">Privacidade</Link></li>
                <li><Link href="/termos" className="hover:opacity-100 transition-opacity">Termos de Uso</Link></li>
                <li><Link href="/cookies" className="hover:opacity-100 transition-opacity">Política de Cookies</Link></li>
                <li><Link href="/lgpd" className="hover:opacity-100 transition-opacity">LGPD</Link></li>
              </ul>
            </div>
          </div>
          
          <div className="border-t border-white/20 pt-8 text-center text-sm opacity-60">
            <p>© 2024 Greencardclub. Todos os direitos reservados.</p>
            <p className="mt-2">CNPJ: 00.000.000/0000-00 | São Paulo - SP</p>
          </div>
        </div>
      </footer>
    </div>
  );
}